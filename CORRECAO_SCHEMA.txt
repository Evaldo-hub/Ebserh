🔧 CORREÇÃO DO SCHEMA SUPABASE - VERSÃO FINAL

📋 PROBLEMA IDENTIFICADO:
Erro: "column desempenho.id_local does not exist"
Causa: Tabelas criadas no Supabase sem os campos de mapeamento para SQLite

✅ SOLUÇÃO IMPLEMENTADA:

1. SCHEMA CORRIGIDO:
- Todas as 4 tabelas agora incluem campos id_local e questao_id_local
- Mapeamento exato: SQLite → Supabase
- Campos adicionais: data_sincronizacao, created_at, updated_at

2. FUNÇÕES NOVAS:
- fix_supabase_tables(): Gera SQL ALTER TABLE para tabelas existentes
- POST /supabase/fix: Endpoint para correção de schema
- Botão "Corrigir Tabelas" na interface

3. MELHORIAS:
- data_resposta com DEFAULT NOW() em vez de NULL
- Índices UNIQUE para id_local
- Índices para performance em questao_id_local

📊 MAPEAMENTO COMPLETO:

SQLite (local) → Supabase (remoto)
=======================================
questoes:
  id INTEGER PRIMARY KEY AUTOINCREMENT → id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY
  + id_local INTEGER UNIQUE (novo campo)

desempenho:
  id INTEGER PRIMARY KEY AUTOINCREMENT → id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY
  questao_id INTEGER NOT NULL → questao_id_local INTEGER NOT NULL
  + id_local INTEGER UNIQUE (novo campo)

plano_estudos:
  id INTEGER PRIMARY KEY AUTOINCREMENT → id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY
  + id_local INTEGER UNIQUE (novo campo)

ia_feedback:
  id INTEGER PRIMARY KEY AUTOINCREMENT → id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY
  questao_id INTEGER NOT NULL → questao_id_local INTEGER NOT NULL
  + id_local INTEGER UNIQUE (novo campo)

🚀 PASSOS PARA CORRIGIR:

OPÇÃO 1 - RECRIAR TABELAS:
1. DROP TABLE questoes, desempenho, plano_estudos, ia_feedback
2. Executar SQL completo do botão "Criar Tabelas"
3. Sincronizar normalmente

OPÇÃO 2 - CORRIGIR TABELAS EXISTENTES:
1. Clicar em "Corrigir Tabelas"
2. Copiar SQL ALTER TABLE gerado
3. Executar no painel SQL do Supabase
4. Sincronizar novamente

✅ RESULTADO:
- Schema 100% compatível com SQLite local
- Sincronização sem erros de coluna
- Mapeamento bidirecional funcional
- Backup completo no Supabase
